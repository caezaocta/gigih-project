import Navbar from '../../components/navbar'
import Typography from '@mui/material/Typography';

const CreatePlaylist = () => {

    return (
        <>
            <Navbar />
            <Typography variant='h3'>CreatePlaylist Page</Typography>
        </>
    );
};

export default CreatePlaylist;